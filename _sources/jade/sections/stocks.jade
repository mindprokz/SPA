| <?php 
|   $args = array(
|     'post_type' => 'stock', //Тип поста
|     'posts_per_page' => -1,//Постов на одной странице
|     'category_name' => 'uncategorized' //Категория постов
|   );
|   $lastBlog = new WP_Query($args);
| ?>
section#stocks
  a(name="stocks")
  .image_slide
    img(src="")
    img(src="").anim_img
  ul.slides
    .wrap
      | <?php
      |  if( $lastBlog->have_posts() ):
      |    while($lastBlog->have_posts() ): $lastBlog->the_post();
      |      $thumbImage =  types_render_field( "stock_photo", array('output' => 'raw') ) ?>
      li.slide.active
        img(src!="<?php echo $thumbImage; ?>")
      | <?php 
      |       $sizeThumb = '';
      |     endwhile;
      |   endif;
      | ?>
